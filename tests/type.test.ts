describe('Type', () => {

    it('Should must declare', () => {

        const name: string = 'Muhammad Eko'
        const balance: number = 1_000_000
        const isVip: boolean = true

        console.info(name)
        console.info(balance)
        console.info(isVip)
    })
})